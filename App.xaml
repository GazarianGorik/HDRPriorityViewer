<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="HDRPriorityViewer.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HDRPriorityViewer">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <Style x:Key="CopyButton" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
            </Style>
            <Style x:Key="CopyButtonCopied" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
            </Style>
            <Style x:Key="RiskyButton" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{ThemeResource SystemFillColorCriticalBackgroundBrush}"/>
            </Style>
            <Style x:Key="ValidateButton" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
