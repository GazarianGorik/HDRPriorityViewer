<ContentDialog
    x:Name="ContentDialog"
    x:Class="HDRPriorityViewer.Views.LoadingDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    BorderThickness="0"
    Title=""
    Loaded="ContentDialog_Loaded">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!-- Carré interne dynamique -->
        <Border x:Name="SquareContainer"
                Background="Transparent"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MinWidth="300"
                MinHeight="200"
                MaxWidth="600"
                MaxHeight="400">
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <ProgressRing Grid.Row="2" Width="60" Height="60" IsActive="True" VerticalAlignment="Top"/>

                <StackPanel Grid.Row="4" Orientation="Vertical" Spacing="5">

                    <TextBlock x:Name="LoadingMessage"
                               Text="Analysing Wwise project..."
                               FontSize="19"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>

                    <TextBlock x:Name="DetailsMessage"
                               Text=""
                               FontSize="11"
                               TextWrapping="Wrap"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Bottom"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ContentDialog>
